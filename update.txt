已修改：
UserCf的fit 使用了新的余弦相似度
dataset的train_test_split
main建立MVCF模型的时候传入名称以便获得用户物品数量来建立二维array
增加了self.records计算MAE RMSE


要填充，最好是新建一个方法。先将trainset（字典）转化数组，（计算余弦相似度）。进行填充，再修改recommend等方法


RMSE、MAE怎么算？
把预测分在recommend中写入self.record，再在test中用真实值计算